export interface LocationData {
    city: {
        name: string;
    };
    province: {
        name: string;
    };
    latitude: string;
    longitude: string;
}

export interface CityData {
    id: number;
    lokasi: string;
}
